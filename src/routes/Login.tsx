import styled from "styled-components";

const Container = styled.div`
  width: 100px;
`;

function Login() {
  return (
    <Container>
      <h1>로그인</h1>
    </Container>
  );
}

export default Login;
